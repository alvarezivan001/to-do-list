import { loadToDoIntoBody } from "./todo-viewer";
import { createToDo, changeDueDate, changePriority} from "./todo-model";

//function for when todo is created
const initializeToDo = (toDo) => {
    
};